[tool.ruff]
line-length = 88
exclude = [
    "nfgda_service/algorithm",
    "backend/src/nfgda_service",
]

[tool.pytest.ini_options]
markers = [
    "slow: marks tests that poll for job completion (deselect with '-m \"not slow\"')",
]
