# This folder is for the setting up of the linter.

    I, William, have the linter set up as of right now. It is using ruff. Ruff is using the defualt which is the following:

    - E pycodestyle errors (basic formatting: whitespace, blank lines, indentation)
    - pyflakes (actual bugs: unused imports, undefined names, unused variables)
